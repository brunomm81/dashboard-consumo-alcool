# Copie este arquivo para .env.local e preencha com os valores reais.
# O .env.local NAO deve ser commitado (esta no .gitignore).

# ---------- Login do dashboard (servidor) ----------
DASHBOARD_USER=
DASHBOARD_PASSWORD=
AUTH_COOKIE_SECRET=

# ---------- Supabase (somente servidor) ----------
SUPABASE_URL=
SUPABASE_DB_PASSWORD=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_SECRET_KEY=
